@model IEnumerable<FrontEnd_MVC.Models.Ville>


@{
    ViewData["Title"] = "AfficheVille";
}
<link href="~/lib/datatables/css/dataTables.bootstrap4.min.css" rel="stylesheet" />
<h1>Liste des villes</h1>

<p>
    <a class="btn btn-primary" asp-action="CreateVille">Create New</a><a class="btn btn-success" asp-action="HomeLoueur">Back to main Menu</a>
</p>
<table id="datatable" class="table">
    <thead>
        <tr>
           
            <th>
                @Html.DisplayNameFor(model => model.Idpays)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th>A un dépôt</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            
            <td>
                @Html.DisplayFor(modelItem => item.IdpaysNavigation.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
                @if (item.Depot.Contains(null))
                {
                  <td>@Html.CheckBox("isUnchecked",new { @disabled = "disabled" })</td>
                    <td>                         
                    <a asp-action="EditVille" asp-route-id="@item.Idville">Edit</a>
                    <a asp-action="deleteVille" asp-route-id="@item.Idville">Delete</a>          
                     </td>
                }
                else
                {
                   <td> @Html.CheckBox("isChecked",true,new { @disabled = "disabled" })</td>
                   <td><i>Can't modify or delete a town with deposit</i></td>                 
                } 
            
        </tr>
}
    </tbody>
</table>

@section Scripts
{
<script src="~/lib/datatables/js/jquery.dataTables.min.js"></script>
<script src="~/lib/datatables/js/dataTables.bootstrap4.min.js"></script>
<script src="~/js/datatable.js"></script>
}